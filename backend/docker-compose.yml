name: acm-certificate-backend

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: acm-certificate-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
